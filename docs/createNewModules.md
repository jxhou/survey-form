# Create new resource using NestJS CLI
  ## Generate resource
  ### Generate Forms resource
    nest generate resource forms
      prompt for transport layer (select REST or GraphQL)
      prompt for CRUD entry points
  ### Generate formFields resource
    nest generate resource formFields

